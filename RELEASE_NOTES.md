# Wee Orchestrator for macOS v0.6.0

Released July 23, 2026.

- Separate Local and Remote environments across navigation and settings.
- Keep the local API running when the app closes, when selected.
- Bound retained task and conversation history for responsive long-running sessions.
- Show streamed tool activity with expandable tool names, inputs, results, and errors.
- Preserve structured tool-event data from the API stream so tool details remain visible.
